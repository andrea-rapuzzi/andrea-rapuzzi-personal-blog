---
---

<section class="hero">
  <div class="hero__inner">
    <h1 class="hero__name">Andrea Rapuzzi</h1>
    <p class="hero__title">CX Lead @Nestlé Waters</p>
    <p class="hero__bio">
      Designer che lavora all'intersezione tra esperienza utente e identità di marca.
      Trasformo complessità in chiarezza, dati in decisioni, interfacce in esperienze memorabili.
    </p>
    <div class="hero__links">
      <a href="/about" class="hero__link hero__link--primary">Chi sono</a>
    </div>
  </div>
  <div class="hero__deco" aria-hidden="true">
    <span class="hero__deco-year">2026</span>
  </div>
</section>

<style>
  .hero {
    padding: 7rem 2rem 5rem;
    max-width: var(--max-width);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: end;
    gap: 2rem;
  }

  .hero__meta {
    margin-bottom: 1.25rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .hero__label {
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-muted);
    border: 1px solid var(--color-border);
    padding: 0.25rem 0.65rem;
    border-radius: 99px;
  }

  .hero__name {
    font-family: var(--font-display);
    font-size: clamp(3rem, 8vw, 6.5rem);
    font-weight: 400;
    line-height: 1.5;
    letter-spacing: 0.0em;
    color: var(--color-heading);
  }

  .hero__title {
    font-family: var(--font-sans);
    font-style: normal;
    font-size: clamp(1.1rem, 2.2vw, 1.75rem);
    font-weight: 300;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-accent);
    margin-top: 0.25rem;
  }

  .hero__bio {
    max-width: 52ch;
    font-size: 1.0625rem;
    line-height: 1.75;
    color: var(--color-muted);
    margin-top: 1.75rem;
  }

  .hero__links {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-top: 2.5rem;
  }

  .hero__link {
    font-size: 0.9375rem;
    font-weight: 500;
    transition: opacity 0.2s;
  }

  .hero__link--primary {
    background: var(--color-ink);
    color: #fff;
    padding: 0.625rem 1.375rem;
    border-radius: 4px;
  }

  .hero__link--primary:hover {
    opacity: 0.8;
  }

  .hero__link:not(.hero__link--primary):hover {
    opacity: 0.6;
  }

  .hero__deco {
    display: flex;
    align-items: end;
    padding-bottom: 0.5rem;
  }

  .hero__deco-year {
    writing-mode: vertical-rl;
    font-size: 0.6875rem;
    font-weight: 300;
    letter-spacing: 0.2em;
    color: var(--color-border);
    user-select: none;
  }

  @media (max-width: 640px) {
    .hero {
      grid-template-columns: 1fr;
      padding-top: 5rem;
    }

    .hero__deco {
      display: none;
    }
  }
</style>
